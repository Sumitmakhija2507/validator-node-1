# ============================================
# SIMPLE VALIDATOR CONFIGURATION
# ============================================
# This validator automatically loads contract addresses
# from your deployment files! No manual config needed.

# Validator Identity
VALIDATOR_ID=validator-1
VALIDATOR_NAME="RapidX Validator #1"
TSS_PARTY_ID=1

# TSS Configuration
TSS_THRESHOLD=3
TSS_TOTAL_PARTIES=5

# Port
PORT=8080

# Optional: Custom RPC URLs (uses public RPCs by default)
# Only add these if you have private RPC endpoints
# SEPOLIA_RPC=https://eth-sepolia.g.alchemy.com/v2/YOUR_KEY
# BSC_TESTNET_RPC=https://data-seed-prebsc-1-s1.binance.org:8545

# Optional: Database for revenue tracking
# Leave empty to disable
# DATABASE_URL=postgresql://user:pass@localhost:5432/rapidx

# Optional: Orchestrator (for multi-validator TSS aggregation)
# Leave empty to run in standalone mode
# ORCHESTRATOR_URL=
# ORCHESTRATOR_WS=

# Optional: Revenue wallet
# REVENUE_WALLET_ADDRESS=0x...
# MIN_PAYOUT_AMOUNT=100

# Log Level
LOG_LEVEL=info
